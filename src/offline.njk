---
layout: base.njk
permalink: /offline/index.html
title: "Offline"
description: "Du er offline. Sitet er tilgængeligt igen, så snart du har forbindelse."
noindex: true
extraHead: |
  <style>
  .offline-wrap {
    min-height: 70vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 120px 24px 80px;
    text-align: center;
    background: var(--sand);
  }
  .offline-inner { max-width: 520px; }
  .offline-eyebrow {
    display: inline-block;
    font-size: 12px;
    font-weight: 500;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 14px;
  }
  .offline-inner h1 {
    font-size: clamp(32px, 5vw, 44px);
    font-style: italic;
    color: var(--petrol);
    margin-bottom: 18px;
  }
  .offline-inner p {
    font-size: 18px;
    color: var(--text);
    line-height: 1.8;
  }
  </style>
---

<section class="offline-wrap">
  <div class="offline-inner">
    <span class="offline-eyebrow">Ingen forbindelse</span>
    <h1>Du er offline lige nu</h1>
    <p>
      Sitet vender tilbage så snart du har internet igen. Du er velkommen til
      at ringe direkte på <a href="tel:{{ site.phoneE164 }}">{{ site.phone }}</a>.
    </p>
  </div>
</section>
